{% extends 'base.html.twig' %}

{% block title %}Ajouter un Produit{% endblock %}

{% block body %}
<div class="container my-5 p-4" 
     style="background: #ffffff; border-radius: 12px; box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1); max-width: 650px;">
    
    <h1 class="text-center mb-4" 
        style="color: #075f5e; font-family: 'Poppins', sans-serif; font-weight: 700; font-size: 2.2rem;">
        🛠 Ajouter un Nouveau produit
    </h1>

    <p class="text-center" style="color: #666; font-size: 1.1rem;">
        Remplissez les détails ci-dessous pour ajouter un nouveau Produit.
    </p>

    <!-- 🌟 Formulaire -->
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    <div class="mb-3">
        <label class="form-label fw-bold"><i class="fas fa-tag"></i> Nom du Produit</label>
        {{ form_widget(form.name, {
            'attr': {'class': 'form-control', 'placeholder': 'Ex: Développement Web'}
        }) }}
    </div>

    <div class="mb-3">
        <label class="form-label fw-bold"><i class="fas fa-align-left"></i> Description</label>
        {{ form_widget(form.description, {
            'attr': {'class': 'form-control', 'placeholder': 'Décrivez le service...'}
        }) }}
    </div>

    <div class="text-center mt-4">
        <button type="submit" class="btn btn-primary btn-lg shadow-sm" 
                style="background-color: #075f5e; border: none; border-radius: 8px; padding: 12px 35px; font-size: 1.2rem;">
            <i class="fas fa-plus-circle"></i> Ajouter
        </button>
    </div>

    {{ form_end(form) }}
</div>
<!-- 🌟 Style amélioré -->
<style>
    .form-control {
        border-radius: 8px;
        border: 1px solid #ddd;
        padding: 12px;
        font-size: 1rem;
        transition: all 0.3s ease-in-out;
    }

    .form-control:focus {
        border-color: #075f5e;
        box-shadow: 0 0 8px rgba(7, 95, 94, 0.2);
    }

    .btn-primary:hover {
        background-color: #064d4c;
        transition: 0.3s ease-in-out;
    }

    .btn-primary i {
        margin-right: 5px;
    }
</style>

{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Accueil | Système de Gestion des Produits{% endblock %}

{% block body %}
<!-- 🎯 HERO SECTION -->
<section class="hero-section text-center py-5 px-4 animate-fade-in" style="background: linear-gradient(135deg, #50b5d7, #74bb78); color: white; border-radius: 12px;">
  <div class="container">
    <h1 class="fw-bold display-4 mb-3" style="font-family: 'Segoe UI', sans-serif;">Bienvenue chez Twinkle-it</h1>
    <p class="lead mb-4" style="font-size: 1.2rem;">Gérez efficacement vos produits, licences et demandes.</p>
    <a href="{{ path('produit_list') }}" class="btn btn-light btn-lg rounded-pill shadow fw-semibold">Voir les Produits</a>
  </div>
</section>
<!-- 🧩 PRODUITS EN VEDETTE -->
<section class="container my-5">
  <h2 class="text-center mb-4 text-primary animate-fade-up">Produits en Vedette</h2>
  <div class="row g-4">
    {% for produit in produits %}
      <div class="col-md-4 animate-fade-up" style="animation-delay: {{ loop.index * 0.1 }}s;">
        <div class="card h-100 shadow border-0 rounded-4 transition-scale">
          <img src="{{ asset('uploads/produits/' ~ produit.image) }}" class="card-img-top" alt="{{ produit.name }}" style="object-fit: cover; height: 220px;">
          <div class="card-body text-center">
            <h5 class="card-title text-secondary">{{ produit.name }}</h5>
            <a href="{{ path('produit_details', {'id': produit.id}) }}" class="btn btn-outline-primary btn-sm rounded-pill">Voir plus</a>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
</section>

<!-- 🛡️ FONCTIONNALITÉS CLÉS -->
<section class="bg-light py-5">
  <div class="container">
    <h2 class="text-center mb-5 text-primary animate-fade-up">Fonctionnalités Clés</h2>
    <div class="row text-center">
      <div class="col-md-4 animate-fade-up">
        <i class="fas fa-key fa-3x mb-3 text-success"></i>
        <h5>Clés Automatiques</h5>
        <p class="text-muted">Création et distribution instantanée de licences.</p>
      </div>
      <div class="col-md-4 animate-fade-up">
        <i class="fas fa-lock fa-3x mb-3 text-primary"></i>
        <h5>Sécurité Renforcée</h5>
        <p class="text-muted">Protection contre le piratage avec vérification via API.</p>
      </div>
      <div class="col-md-4 animate-fade-up">
        <i class="fas fa-user-cog fa-3x mb-3 text-dark"></i>
        <h5>Gestion Client</h5>
        <p class="text-muted">Suivi des demandes et visualisation des licences.</p>
      </div>
    </div>
  </div>
</section>

<!-- 🗣️ TÉMOIGNAGES -->
<section class="container my-5">
  <h2 class="text-center mb-4 text-primary animate-fade-up">Témoignages</h2>
  <div class="row g-4">
    {% for temoignage in [
      {'message': 'Un outil efficace pour gérer nos licences en toute simplicité.', 'auteur': 'A. B.'},
      {'message': 'Une interface claire, intuitive et fluide. Bravo !', 'auteur': 'M. T.'},
      {'message': 'Une solution moderne et bien pensée.', 'auteur': 'C. K.'}
    ] %}
      <div class="col-md-4 animate-fade-up">
        <div class="bg-white shadow-sm p-4 rounded-3 h-100 hover-scale">
          <p class="fst-italic">"{{ temoignage.message }}"</p>
          <p class="fw-bold mt-3 text-end">– {{ temoignage.auteur }}</p>
        </div>
      </div>
    {% endfor %}
  </div>
</section>

<!-- ❓ FAQ -->
<section class="container my-5">
  <h2 class="text-center text-primary mb-4 animate-fade-up">FAQ</h2>
  <div class="accordion accordion-flush" id="faqAccordion">
    <div class="accordion-item">
      <h2 class="accordion-header" id="faq1-heading">
        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq1">
          Comment obtenir une licence ?
        </button>
      </h2>
      <div id="faq1" class="accordion-collapse collapse">
        <div class="accordion-body">Depuis votre profil client, envoyez une demande selon votre produit.</div>
      </div>
    </div>
    <div class="accordion-item">
      <h2 class="accordion-header" id="faq2-heading">
        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq2">
          Est-ce sécurisé ?
        </button>
      </h2>
      <div id="faq2" class="accordion-collapse collapse">
        <div class="accordion-body">Oui, notre système communique uniquement via HTTPS avec vérification automatique.</div>
      </div>
    </div>
  </div>
</section>
{% endblock %}

{% block stylesheets %}
<style>
  h1, h2, h3, h4, h5 {
    font-family: 'Segoe UI', 'Open Sans', sans-serif;
  }
  .btn-outline-primary {
    color: #50b5d7;
    border-color: #50b5d7;
  }
  .btn-outline-primary:hover {
    background-color: #50b5d7;
    color: white;
  }
  .transition-scale, .hover-scale {
    transition: transform 0.3s ease;
  }
  .transition-scale:hover, .hover-scale:hover {
    transform: scale(1.05);
  }
  .animate-fade-in {
    animation: fadeIn 1s ease-in-out;
  }
  .animate-fade-up {
    animation: fadeInUp 1s ease-in-out;
  }
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }
</style>
{% endblock %}

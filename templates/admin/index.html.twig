{% extends 'base.html.twig' %}

{% block title %}Dashboard Admin{% endblock %}

{% block body %}
<style>
    .dashboard-wrapper {
        background: linear-gradient(135deg, #f0f4f7, #ffffff);
        padding: 40px 20px;
        border-radius: 15px;
    }

    .dashboard-card {
        border: none;
        border-radius: 15px;
        background-color: white;
        box-shadow: 0 4px 10px rgba(0,0,0,0.07);
        transition: 0.3s ease;
    }

    .dashboard-card:hover {
        transform: scale(1.02);
    }

    .dashboard-card h3 {
        font-size: 2.5rem;
        font-weight: bold;
    }

    .dashboard-card p {
        font-size: 1.1rem;
        margin-top: 5px;
        color: #666;
    }

    .btn-custom {
        border-radius: 25px;
        font-weight: bold;
        font-size: 1rem;
        padding: 10px 25px;
    }

    .text-primary { color: #2F93B9 !important; }
    .text-success { color: #2AB572 !important; }
    .text-warning { color: #f5a623 !important; }
    .text-info { color: #17a2b8 !important; }
</style>

<div class="container dashboard-wrapper">
    <h1 class="text-center text-primary mb-5">👨‍💼 Dashboard Administrateur</h1>

    <div class="row text-center">
        <div class="col-md-4 mb-4">
            <div class="card dashboard-card p-4">
                <h3 class="text-success">{{ nbUsers }}</h3>
                <p>Utilisateurs Inscrits</p>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="card dashboard-card p-4">
                <h3 class="text-info">{{ nbLicenses }}</h3>
                <p>Licences Actives</p>
            </div>
        </div>
        <div class="col-md-4 mb-4">
            <div class="card dashboard-card p-4">
                <h3 class="text-warning">{{ revenus|number_format(2, ',', ' ') }} TND</h3>
                <p>Revenus Totaux</p>
            </div>
        </div>
    </div>

    <div class="text-center mt-4">
        <a href="{{ path('admin_users') }}" class="btn btn-outline-primary btn-custom m-2">📋 Gérer Utilisateurs</a>
        <a href="{{ path('admin_licenses') }}" class="btn btn-outline-info btn-custom m-2">🔑 Gérer Licences</a>
        <a href="{{ path('admin_paiements') }}" class="btn btn-outline-success btn-custom m-2">💰 Voir Paiements</a>
    </div>
</div>
{% endblock %}

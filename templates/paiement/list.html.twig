{% extends 'base.html.twig' %}

{% block title %}Liste des Paiements{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="text-center text-primary">📜 Liste des Paiements</h1>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Utilisateur</th>
                <th>Montant</th>
                <th>Date</th>
                <th>Licence</th>
            </tr>
        </thead>
        <tbody>
            {% for paiement in paiements %}
                <tr>
                    <td>{{ paiement.id }}</td>
                    <td>{{ paiement.utilisateur.email }}</td>
                    <td>{{ paiement.montant }} €</td>
                    <td>{{ paiement.datePaiement|date('d/m/Y H:i') }}</td>
                    <td>{{ paiement.license.nom }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
